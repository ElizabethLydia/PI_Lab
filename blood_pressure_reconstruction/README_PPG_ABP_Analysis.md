# PPG-ABP信号分析系统详细说明文档

## 📋 系统概述

本系统是一个全面的PPG（光电容积脉搏波）和ABP（动脉血压）信号分析平台，集成了**15种不同的信号预处理和提取方法**。系统专门为受试者00017设计，支持多实验、多传感器的批量分析。

## 🎯 主要功能

- **多通道数据支持**: IR、Red、Green三个PPG通道
- **15种预处理方法**: 从传统滤波到现代机器学习算法
- **自适应采样率**: 自动从时间戳检测真实采样率
- **运动伪影检测**: 基于加速度数据的智能运动检测
- **相关性分析**: 多种相关性指标计算
- **信噪比评估**: 基于BVPSNR方法的专业SNR计算
- **可视化输出**: 三列布局的详细对比图表

## 📊 数据流程

```
原始数据 → 信号对齐 → 预处理 → 相关性计算 → 可视化输出
```

## 🔧 详细方法说明

### 1. 原始信号 (Original Signal)
**描述**: 未经任何处理的原始PPG信号
**处理**: 无
**适用场景**: 基线对比，评估预处理效果
**优势**: 保持原始信号特征
**劣势**: 包含噪声和伪影

### 2. Butterworth滤波 (Butterworth Filter)
**描述**: 经典的数字滤波器，使用无限冲激响应(IIR)设计
**参数**: 
- 低截止频率: 0.5 Hz
- 高截止频率: 4.0 Hz  
- 滤波器阶数: 4
- 采样率: **自动检测** (从时间戳计算)
**数学原理**: 
```
H(s) = 1 / √(1 + (s/jωc)^(2n))
```
其中ωc是截止频率，n是滤波器阶数
**采样率检测**:
```
fs = 1.0 / mean(time_diff)
其中time_diff是相邻时间戳的差值
```
**适用场景**: 去除基线漂移和高频噪声
**优势**: 相位响应线性，群延迟恒定，采样率自适应
**劣势**: 通带和阻带过渡较慢

### 3. 小波去噪 (Wavelet Denoising)
**描述**: 基于小波变换的多分辨率信号分析
**参数**:
- 小波基函数: db4 (Daubechies 4阶)
- 分解层数: 4
- 阈值方法: 软阈值
**数学原理**:
```
阈值 = σ × √(2 × log(N))
其中σ是噪声标准差，N是信号长度
```
**处理步骤**:
1. 小波分解到4层
2. 计算阈值
3. 软阈值处理高频系数
4. 小波重构
**适用场景**: 去除随机噪声，保持信号特征
**优势**: 时频局部化好，适应性强
**劣势**: 小波基选择影响效果

### 4. 形态学滤波 (Morphological Filter)
**描述**: 基于数学形态学的非线性滤波方法
**参数**:
- 结构元素大小: 5
- 操作类型: 开运算+闭运算
**数学原理**:
```
开运算: opened = grey_opening(signal, size=5)
闭运算: closed = grey_closing(opened, size=5)
```
**处理步骤**:
1. 开运算：先腐蚀后膨胀，去除峰值
2. 闭运算：先膨胀后腐蚀，填充谷值
**适用场景**: 去除基线漂移，平滑信号
**优势**: 保持信号形状特征
**劣势**: 可能过度平滑

### 5. PCA去噪 (PCA Denoising - Bandpass)
**描述**: **先带通滤波，再进行PCA降噪**的改进方法
**参数**:
- 主成分数: 3
- 分段长度: 100点
- 预处理: Butterworth带通滤波 (0.5-4Hz)
**数学原理**:
```
步骤1: 带通滤波
filtered = butterworth_filter(signal, lowcut=0.5, highcut=4.0)

步骤2: PCA分解
X = U × Σ × V^T
X_reconstructed = U[:, :k] × Σ[:k, :k] × V^T[:k, :]
```
**处理步骤**:
1. **带通滤波预处理** (0.5-4Hz)
2. 信号分段（每段100点）
3. 构建信号矩阵
4. PCA分解
5. 保留前3个主成分
6. 重构信号
**适用场景**: 去除相关噪声，保持主要特征
**优势**: 先滤波去噪，再PCA降维，效果更好
**劣势**: 计算复杂度增加

### 6. 卡尔曼滤波 (Kalman Filter)
**描述**: 基于状态估计的最优滤波算法
**参数**:
- 过程噪声协方差Q: 0.1
- 测量噪声协方差R: 1.0
**数学原理**:
```
预测步骤:
x̂_k^- = x̂_{k-1}
P_k^- = P_{k-1} + Q

更新步骤:
K_k = P_k^- / (P_k^- + R)
x̂_k = x̂_k^- + K_k × (z_k - x̂_k^-)
P_k = (1 - K_k) × P_k^-
```
**处理步骤**:
1. 初始化状态估计和协方差
2. 预测步骤
3. 更新步骤
4. 迭代处理所有数据点
**适用场景**: 实时信号滤波，噪声抑制
**优势**: 最优估计，自适应增益
**劣势**: 需要噪声模型

### 7. 组合滤波 (Combined Filter)
**描述**: Butterworth滤波 + 小波去噪的组合方法
**处理步骤**:
1. 先应用小波去噪
2. 再应用Butterworth滤波
**适用场景**: 需要多级滤波的复杂噪声环境
**优势**: 结合两种方法优势
**劣势**: 计算复杂度增加

### 8. 改进运动伪影去除 (Motion Removal - Bandpass)
**描述**: **先带通滤波，再进行运动检测和去除**的改进方法
**参数**:
- 窗口大小: 100点
- 阈值倍数: 1.5
- 预处理: Butterworth带通滤波 (0.5-4Hz)
**数学原理**:
```
步骤1: 带通滤波预处理
filtered_ppg = butterworth_filter(ppg_signal, lowcut=0.5, highcut=4.0)

步骤2: 加速度幅度计算
acc_magnitude = √(ax² + ay² + az²)

步骤3: 动态阈值
threshold = local_mean + 1.5 × local_std
```
**处理步骤**:
1. **带通滤波预处理** (0.5-4Hz)
2. 计算加速度幅度
3. 滑动窗口动态阈值计算
4. 形态学操作去除孤立点
5. 三次样条插值重构运动段
**适用场景**: 运动环境下的PPG信号
**优势**: 先滤波去噪，再运动检测，效果更准确
**劣势**: 需要加速度数据

### 9. 多通道融合 (Multi-Channel Fusion)
**描述**: 加权融合IR、Red、Green三个通道
**权重分配**:
- IR通道: 0.6 (主要信号)
- Red通道: 0.3 (辅助信号)  
- Green通道: 0.1 (参考信号)
**数学原理**:
```
PPG_fusion = 0.6 × IR + 0.3 × Red + 0.1 × Green
```
**适用场景**: 多通道数据可用时
**优势**: 充分利用多通道信息
**劣势**: 权重需要经验调整

### 10. PCA提取 (PCA Extraction)
**描述**: 基于主成分分析的PPG信号提取
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. PCA分解: X = U × Σ × V^T
3. 选择第一主成分: PPG = U[:, 0] × Σ[0, 0]
```
**适用场景**: 多通道PPG信号分离
**优势**: 自动降维，保持主要特征
**劣势**: 线性变换限制

### 11. SVD提取 (SVD Extraction)
**描述**: 基于奇异值分解的信号分离
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. SVD分解: X = U × Σ × V^T
3. 第一奇异值对应: PPG = U[:, 0] × Σ[0]
```
**适用场景**: 矩阵分解和信号分离
**优势**: 数值稳定性好
**劣势**: 计算复杂度高

### 12. POS提取 (POS Extraction)
**描述**: 皮肤正交平面算法
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. 归一化: r_norm = R / mean(R), g_norm = G / mean(G), b_norm = B / mean(B)
3. POS信号: PPG = (r_norm - g_norm) + (r_norm - b_norm)
4. 再次带通滤波
```
**适用场景**: 接触式PPG
**优势**: 物理意义明确，计算简单
**劣势**: 假设条件严格

### 13. CHROM提取 (CHROM Extraction)
**描述**: 基于色度的PPG信号提取
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. 归一化RGB值
3. 色度信号: PPG = (r_norm - g_norm) / (r_norm + g_norm - 2×b_norm)
4. 再次带通滤波
```
**适用场景**: 非接触式PPG
**优势**: 抗光照变化
**劣势**: 对颜色通道敏感

### 14. ICA提取 (ICA Extraction)
**描述**: 独立分量分析
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. FastICA算法分离独立分量
3. 选择方差最大的分量
```
**适用场景**: 运动环境，多传感器
**优势**: 自动分离，去噪效果好
**劣势**: 计算复杂，需要多通道

### 15. NMF提取 (NMF Extraction)
**描述**: 非负矩阵分解
**数学原理**:
```
1. 预处理RGB通道（归一化 + 带通滤波）
2. 确保数据非负
3. NMF分解: X ≈ W × H
4. 选择重构误差最小的分量
5. 再次带通滤波
```
**适用场景**: 非负数据分解
**优势**: 物理意义明确
**劣势**: 收敛速度慢

## 📈 相关性指标

### Pearson相关系数
- **范围**: [-1, 1]
- **含义**: 线性相关性
- **目标**: r > 0.9 (根据研究论文)

### Spearman相关系数
- **范围**: [-1, 1]  
- **含义**: 单调相关性
- **优势**: 对异常值不敏感

### 互信息 (Mutual Information)
- **范围**: [0, ∞)
- **含义**: 非线性依赖性
- **优势**: 捕捉复杂关系

### 频率域相关性
- **计算**: FFT幅值相关性
- **含义**: 频域相似性

### 相干性 (Coherence)
- **范围**: [0, 1]
- **含义**: 频域线性相关性
- **方法**: scipy.signal.coherence

## 🎯 信噪比计算 (BVPSNR)

### 计算方法
基于G. de Haan, TBME, 2013的方法：

```
1. 计算功率谱密度 (periodogram)
2. 心率峰值区域: ±0.1 Hz
3. 一次谐波区域: ±0.2 Hz  
4. 信号功率 = 心率功率 + 谐波功率
5. 总功率: 0.5-4 Hz范围
6. SNR = 10 × log10(信号功率 / 噪声功率)
```

### 参数设置
- 采样率: 100 Hz
- 频率分辨率: 0.5 BPM
- 心率范围: 0.5-4 Hz (30-240 BPM)

## 🏃 运动检测

### 检测原理
```
加速度幅度 = √(ax² + ay² + az²)
阈值 = 局部均值 + 1.5 × 局部标准差
运动掩码 = 加速度幅度 > 阈值
```

### 处理策略
1. 滑动窗口动态阈值
2. 形态学操作去噪
3. 三次样条插值重构

## 📁 输出文件结构

```
/root/autodl-tmp/blood_pressure_reconstruction/
└── 00017/
    └── analysis_results/
        ├── 00017_exp1_sensor2_full_length_signals.png
        ├── 00017_exp1_sensor2_segment_signals.png
        └── 00017_exp1_sensor2_correlation_heatmap.png
```

## 🚀 使用方法

### 单个分析
```bash
python ppg_abp_analysis_00017.py
# 选择模式1，输入实验编号和传感器名称
```

### 批量分析
```bash
echo "2" | python ppg_abp_analysis_00017.py
# 自动处理所有实验和传感器
```

## ⚠️ 注意事项

1. **数据质量**: 确保PPG和ABP数据完整对齐
2. **内存使用**: 长信号段可能需要较大内存
3. **计算时间**: 某些方法（如ICA、NMF）计算较慢
4. **参数调优**: 根据具体数据调整滤波参数

## 🔬 技术特点

- **模块化设计**: 易于添加新的预处理方法
- **异常处理**: 优雅处理算法失败情况
- **内存优化**: 避免不必要的数据复制
- **可视化友好**: 支持服务器环境和GUI环境

## 📚 参考文献

1. G. de Haan, "Robust pulse rate from chrominance-based rPPG," IEEE Trans. Biomed. Eng., 2013
2. M. Z. Poh, "Non-contact, automated cardiac pulse measurements using video imaging and blind source separation," Opt. Express, 2010
3. W. Verkruysse, "Remote plethysmographic imaging using ambient light," Opt. Express, 2008

---

*本系统专为PPG-ABP信号分析研究设计，支持多种预处理方法和评估指标。*
